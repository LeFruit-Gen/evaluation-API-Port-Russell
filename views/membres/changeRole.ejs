<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier le rôle - <%= user.username %></title>
</head>
<body>
    <%- contentFor('body') %>
    <div class="container">
        <div class="page-header">
            <h1 class="page-title">Modifier le rôle de <%= user.username %></h1>
            <a href="/users" class="btn btn-secondary">← Retour aux utilisateurs</a>
        </div>
        <div class="form-container">
            <table class="info-table">
                <tr>
                    <th>Nom d'utilisateur</th>
                    <td><%= user.username %></td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td><%= user.email %></td>
                </tr>
                <tr>
                    <th>Rôle actuel</th>
                    <td><%= user.role || 'user' %></td>
                </tr>
            </table>
            <form action="/users/<%= user.email %>/role" method="POST" class="mt-4">
                <input type="hidden" name="_method" value="PUT">
                
                <div class="form-group">
                    <label for="role">Nouveau rôle</label>
                    <select name="role" id="role" class="form-control" required>
                        <option value="user" <%= (user.role === 'user' || !user.role) ? 'selected' : '' %>>Utilisateur</option>
                        <option value="admin" <%= user.role === 'admin' ? 'selected' : '' %>>Administrateur</option>
                    </select>
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">Enregistrer</button>
                    <a href="/users" class="btn btn-secondary">Annuler</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
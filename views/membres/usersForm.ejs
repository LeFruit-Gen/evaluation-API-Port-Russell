<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= isEditing ? 'Modifier' : 'Créer' %> un utilisateur</title>
</head>
<body>
    <%- contentFor('body') %>
    <div class="container">
        <div class="page-header">
            <h1 class="page-title"><%= isEditing ? 'Modifier' : 'Créer' %> un utilisateur</h1>
            <a href="/users" class="btn btn-secondary">← Retour aux utilisateurs</a>
        </div>
        <% if (message) { %>
            <div class="alert alert-info">
                <%= message %>
            </div>
        <% } %>
        <div class="form-container">
            <form action="/users/<%= isEditing ? user.email : '' %>" method="POST">
                <% if (isEditing) { %>
                    <input type="hidden" name="_method" value="PUT">
                <% } %>
                <div class="form-group">
                    <label for="username">Nom d'utilisateur</label>
                    <input type="text" id="username" name="username" class="form-control" 
                           value="<%= user ? user.username : '' %>" required>
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" class="form-control" 
                           value="<%= user ? user.email : '' %>" required>
                </div>
                <div class="form-group">
                    <label for="password">Mot de passe</label>
                    <input type="password" id="password" name="password" class="form-control" 
                           <%= !isEditing ? 'required' : '' %>>
                    <% if (isEditing) { %>
                        <small class="form-text text-muted">Laissez vide pour conserver le mot de passe actuel</small>
                    <% } %>
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">
                        <%= isEditing ? 'Modifier' : 'Créer' %> l'utilisateur
                    </button>
                    <a href="/users" class="btn btn-secondary">Annuler</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
